{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "contracts/cli/v1/ai_new_list.schema.json",
  "title": "ai new list response v1",
  "type": "object",
  "required": ["drafts"],
  "properties": {
    "drafts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "draftId",
          "schema",
          "id",
          "title",
          "filename",
          "createdAt",
          "expiresAt",
          "ttlSeconds"
        ],
        "properties": {
          "draftId": { "type": "string" },
          "schema": { "type": "string" },
          "id": { "type": "string" },
          "title": { "type": "string" },
          "filename": { "type": "string" },
          "createdAt": { "type": "string", "format": "date-time" },
          "expiresAt": { "type": "string", "format": "date-time" },
          "ttlSeconds": { "type": "integer", "minimum": 0 }
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
